---
# HA Add-on builder configuration
# https://developers.home-assistant.io/docs/add-ons/configuration#add-on-build-configuration

# The Dockerfile is a self-contained multi-stage build
# (node:20-alpine builder → node:20-alpine server) so we do not override BUILD_FROM per arch.

labels:
  org.opencontainers.image.title: 'HA React Dashboard'
  org.opencontainers.image.description: 'Responsive React dashboard for Home Assistant'
  org.opencontainers.image.source: 'https://github.com/thomasv04/ha-react-dashboard'
  org.opencontainers.image.licenses: 'MIT'
